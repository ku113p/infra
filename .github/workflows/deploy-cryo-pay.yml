name: Deploy Cryo Pay

on:
  push:
    branches: [master]
    paths:
      - 'compose/cryo-pay/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.VPS_SSH_KEY }}

      - name: Add VPS to known hosts
        run: ssh-keyscan -H ${{ secrets.VPS_HOST }} >> ~/.ssh/known_hosts

      - name: Sync compose files
        run: |
          SERVER="root@${{ secrets.VPS_HOST }}"
          rsync -avz --delete compose/cryo-pay/ ${SERVER}:/opt/services/cryo-pay/ --exclude .env

      - name: Restart services
        run: |
          SERVER="root@${{ secrets.VPS_HOST }}"
          ssh ${SERVER} "cd /opt/services/cryo-pay && docker compose up -d"

      - name: Verify health
        run: |
          SERVER="root@${{ secrets.VPS_HOST }}"
          sleep 15
          RUNNING=$(ssh ${SERVER} "docker ps --filter 'name=cryo-pay' --format '{{.Names}}' | wc -l")
          if [ "$RUNNING" -lt 5 ]; then
            echo "FAIL: expected 5 cryo-pay containers, found $RUNNING"
            ssh ${SERVER} "docker ps -a --filter 'name=cryo-pay' --format 'table {{.Names}}\t{{.Status}}'"
            exit 1
          fi
          ssh ${SERVER} "docker ps --filter 'name=cryo-pay' --format 'table {{.Names}}\t{{.Status}}\t{{.Ports}}'"
